node_modules/
dist/
*.log
.DS_Store


# Added by cargo

/target
__pycache__/
/.herdr-reference
# Zig build/package caches for libghostty-vt, kept outside target/ so CI's
# Rust cache never archives them (see build.rs for why they must stay fresh).
/.zig-cache-ah
