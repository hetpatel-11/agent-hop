{
  "name": "agent-hop-darwin-arm64",
  "version": "0.1.5",
  "description": "agent-hop native binary for macOS (Apple Silicon). Not meant to be installed directly -- installed automatically as an optionalDependency of the `agent-hop` package.",
  "license": "MIT",
  "os": ["darwin"],
  "cpu": ["arm64"],
  "files": ["bin/ah"]
}
