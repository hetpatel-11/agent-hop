{
  "name": "agent-hop-darwin-x64",
  "version": "0.1.3",
  "description": "agent-hop native binary for macOS (Intel). Not meant to be installed directly -- installed automatically as an optionalDependency of the `agent-hop` package.",
  "license": "MIT",
  "os": ["darwin"],
  "cpu": ["x64"],
  "files": ["bin/ah"]
}
