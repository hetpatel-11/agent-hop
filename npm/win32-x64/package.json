{
  "name": "agent-hop-windows-x64",
  "version": "0.1.6",
  "description": "agent-hop native binary for Windows (x64). Not meant to be installed directly -- installed automatically as an optionalDependency of the `agent-hop` package.",
  "license": "MIT",
  "os": ["win32"],
  "cpu": ["x64"],
  "files": ["bin/ah.exe"]
}
